if = {
	limit = {
		check_variable = { which = suelg_res_dice value <= suelg_$RES$_weight }
		NOT = { has_planet_flag = new_deposit_added }
	}
	log = ">> DEPOSIT SPAWN: $DEP$ WEIGHT: [this.suelg_$RES$_weight]"
	set_planet_flag = new_deposit_added
	add_deposit = $DEP$
} else = {
	subtract_variable = { which = suelg_res_dice value = suelg_$RES$_weight }
}